# Aether OS — ps command
# Standalone binary: lists running processes

import "../lib/libaether.ae"

func main(): u64 {
    puts("PID  NAME\n")
    puts("  1  kernel\n")
    puts("  2  shell\n")
    return 0
}
