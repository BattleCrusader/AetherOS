# Aether OS — echo command
# Standalone binary
import "../lib/libsys.ae"

func main(inputString: string): u64 {
    if inputString != 0 {
        puts("hello from echo ")
        puts(inputString)
        puts("\n")
    } else {
        puts("hello from echo\n")
    }
    return 0
}
