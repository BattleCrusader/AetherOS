# Aether OS — echo command
# Standalone binary
import "../lib/libaether.ae"

func main(inputString: string): u64 {
    try {
        puts("hello from echo {inputString}\n")
    } catch _ {
        puts("Exception from echo\n")
    }
    return 0
}
